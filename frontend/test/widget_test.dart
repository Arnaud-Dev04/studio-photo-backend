// Ce fichier de test par défaut est un placeholder.
// Les tests seront ajoutés au fur et à mesure du développement.
import 'package:flutter_test/flutter_test.dart';

void main() {
  testWidgets('Application démarre correctement', (WidgetTester tester) async {
    // Test de base — sera enrichi plus tard
    expect(true, isTrue);
  });
}
